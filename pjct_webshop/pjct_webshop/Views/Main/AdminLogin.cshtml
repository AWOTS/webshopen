@model pjct_webshop.Models.User
@{
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}

@using (Html.BeginForm("AdminLogin", "Main", FormMethod.Post))
{

    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    <table>
        <tr>
            <td>@Html.LabelFor(a => a.Username)</td>
            <td>@Html.TextBoxFor(a => a.Username)</td>
            <td>@Html.ValidationMessageFor(a => a.Username)</td>
        </tr>
        <tr>
            <td>
            <td>@Html.LabelFor(a => a.Password)</td>
            <td>@Html.TextBoxFor(a => a.Password)</td>
            <td>@Html.ValidationMessageFor(a => a.Password)</td>
        </tr>
        <tr>
            <td></td>
            <td>
                <input type="submit" value="Logga in" />
            </td>
        </tr>
    </table>
}

@*<div class="container">
    <div class="row">
        <div class="col-lg-12 text-center SmileWithStyleCopyright">
            <p>
                Admin login
                <span class="glyphicon glyphicon-log-in"> </span>
            </p>
            <p>
                <fieldset>
                    <label for="email">Namn</label>
                    <input type="text" name="email" id="email" value="Name / Email" class="text ui-widget-content ui-corner-all">
                    <label for="password">Lösenord</label>
                    <input type="password" name="password" id="password" value="********" class="text ui-widget-content ui-corner-all">
                    <br />
                    <br />
                    <br />
                    <input type="submit" value="Logga in" />
                </fieldset>
            </p>
        </div>
    </div>
</div>*@
}


